import 'package:flutter/material.dart';

class LiveClassSetupWidget extends StatelessWidget {
  const LiveClassSetupWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
